# Synced by the Probot Settings app: https://probot.github.io/apps/settings/
# Repository-level settings only. Branch protection is deliberately absent:
# scripts/apply-repo-safeguards.sh owns its transactional cutover and recovery.

repository:
  allow_merge_commit: false
  allow_squash_merge: true
  allow_rebase_merge: false
  delete_branch_on_merge: true
  enable_automated_security_fixes: true
  enable_vulnerability_alerts: true
