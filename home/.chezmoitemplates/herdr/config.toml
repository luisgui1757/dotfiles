# Managed by dotfiles. Herdr's built-in Rose Pine theme keeps its UI aligned
# with the rest of the terminal stack on every supported host.
onboarding = false

[theme]
name = "rose-pine"
auto_switch = false
