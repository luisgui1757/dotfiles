# v0.2.0 — Nix package ownership and transactional v0.1 migration

> Published 2026-07-15 as immutable GitHub release `354480554`. Annotated tag
> object `cd9a60436b3064c5e2f6ed5bfd8ae0f5297f1b49` peels to commit
> `22cfad80e904e003f52932ae6d6403520df00d3c`.

## Breaking contract changes

- Apple Silicon is the sole macOS architecture in the v0.2.0 product contract;
  no other Darwin configuration, installer path, or migration path is shipped.
- Nix owns the required POSIX package layer: nix-darwin on macOS and
  package-only Home Manager on Linux/WSL. Chezmoi still owns every config
  target. Native Windows remains non-Nix. macOS setup retries are safe before
  opening a refreshed shell: the installed current-system rebuild command is
  resolved outside stale `PATH`, and nix-darwin-managed `/etc/static` shell
  links keep their retained recovery backups. Legacy tap transaction and
  failed-output roots stay outside Homebrew's scanned `Library/Taps` tree;
  setup automatically relocates exact recovery artifacts left there by the
  broken predecessor before retrying activation. Setup also recovers a present
  canonical Nix profile binary after Homebrew path refresh, even if the
  upstream profile's already-sourced guard makes re-sourcing a no-op.
- WSL is explicitly split-host: Windows setup owns host Terminal/font/clipboard
  pieces and the distro owns its Linux Nix/config state.
- Windows resolves UserProfile, LocalApplicationData, Documents, and the active
  PowerShell profile independently, including redirected and alternate-drive
  known folders. Stable packaged, Preview, Canary, and portable Windows
  Terminal settings are separate transactions driven by one shared target
  identity schema.
- Required CI identities move from runner-image names to stable logical names
  through a post-merge owner transaction; both name sets are emitted during the
  transition.

## Upgrade requirement

Do not update a v0.1.0 checkout in place. Its POSIX targets are live symlinks
into that checkout, so changing Git first crosses the config mutation boundary
before backup.

Clone v0.2.0 beside v0.1.0 and run the ordinary `./setup.sh --all` or
`.\setup.ps1 -All` entry point from the new checkout. Setup owns the complete
path documented in [`docs/UPGRADING.md`](../UPGRADING.md): it bootstraps verified
Nix on POSIX when needed, verifies both annotated releases and the live
historical config, captures private recovery, runs the reversible core, resumes
an already-applied recovery safely, verifies and accepts it under the explicit
all-mode contract, then completes additive provisioning and config repointing.
The standalone migration commands remain operator recovery tools, not required
user steps.
Failure or interruption after package activation restores the prior config and
removes the first nix-darwin/Home Manager activation. Windows restores exact
pre-migration Terminal bytes and v0.1.0 config from its validated frozen release
tree, so rollback does not read mutable checkout bytes.

## Release identity

The checked-in migration tools require the tag name `v0.2.0` and compare its
local annotated tag object and peeled commit to the official remote at runtime;
there is no pre-release commit placeholder to trust. They reject a branch,
lightweight tag, dirty checkout, unofficial remote, or moved tag. The observed
tag object and peeled commit are recorded in the append-only review ledger only
after GitHub publishes them.

## Publication evidence and residual manual proof

- Passed: full local `make ci` on the exact release commit.
- Passed: exact v0.1.0 deterministic fixture
  (`015617362830280bf85c7142e69d0681d376d453`) through setup-owned bootstrap,
  apply, injected later failure, rollback, retry/resume, acceptance, and final
  reconciliation.
- Passed: cache-free hosted release run
  [`29419942595`](https://github.com/luisgui1757/dotfiles/actions/runs/29419942595)
  across Ubuntu, Apple Silicon macOS, Windows, the Linux container, and all four
  stable logical proofs. Both POSIX lanes logged the exact immutable v0.2.0 tag
  identity.
- Passed: redacted Gitleaks 8.30.1 scan across all 13 commits in
  `v0.1.0..v0.2.0` and the four downloaded stable logical proof artifacts; no
  leaks found.
- Passed: live safeguard readback requires the twelve stable logical contexts
  and reports `sha_pinning_required: true`.
- Open: real Apple Silicon owner-host, physical Linux, WSL2 split-host,
  redirected Windows, divergent stable packaged/Preview/Canary/portable
  Terminal, and desktop/visual rows in `tests/MANUAL.md`.

The owner explicitly authorized publication with those real-environment rows
still open. Their unchecked state remains a post-release evidence gap and is
not claimed as a pass.
