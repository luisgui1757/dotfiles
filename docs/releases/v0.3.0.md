# v0.3.0 — Pi Rose Pine themes and resilient cross-platform setup

> Published 2026-07-19 as immutable GitHub release `356273955`. Annotated tag
> object `473f675e863640484d4d11349bf69d01def12c43` peels to commit
> `c8507312153620b9b30fe2c84980c62bccb3b25a`.

## Highlights

- Pi now installs three canonical Fable-tuned Rose Pine themes: Main
  (`rose-pine`, still the first-setup default), Moon, and Dawn. Setup preserves
  a selected managed variant, safely retires recognized trial aliases, and
  keeps the exact multiline-input keybindings for Shift+Enter and Ctrl+J.
- Herdr uses its corrected indexed-workspace navigation across supported
  platforms. Native Windows opens managed panes with PowerShell 7 so the repo's
  profile, history prediction, completion, and prompt load consistently.
- POSIX Nix bootstrap now avoids upstream shell-profile mutation, skips the
  unused mutable channel, repairs restrictive store modes left by interrupted
  daemon installs, and retains the explicit verified branch-head field-test
  lane without weakening the exact-release default.
- Pinned tooling moves to Pi `0.80.10`, Tree-sitter CLI `0.26.11`, gh-dash
  `v4.25.2`, lazygit `v0.63.1`, Windows Terminal portable `v1.24.11911.0`,
  `setuptools` `83.0.0`, Renovate `43.270.0`, and the reviewed installer
  commits/hashes recorded in the supply-chain ledger.

## Compatibility and upgrade

This is a backward-compatible minor release: it adds user-visible Pi theme
functionality and Herdr behavior while retaining the public setup commands and
managed config shapes. No breaking contract change is introduced.

Do not update an older checkout in place. Clone the exact annotated `v0.3.0`
tag beside the checkout that currently owns live config, then run:

```bash
./setup.sh --all
```

```powershell
.\setup.ps1 -All
```

The same entrypoints support a direct side-by-side v0.1.0 migration through the
versioned frozen-source transaction. Existing v0.2.0 installations use the
ordinary setup/update reconciliation from the new checkout. Keep the prior
checkout and any printed recovery directory until the new setup completes and
the applications you use have been checked.
An unfinished v0.2.0 recovery must be accepted or rolled back from its retained
exact v0.2.0 checkout first; v0.3.0 setup refuses to bypass it.

## Release identity

The POSIX prerequisite helper and both v0.1.0 migrators require the tag name
`v0.3.0` and accept only the exact local annotated tag object, peeled commit,
clean HEAD, and matching official-remote identities. The explicit
`--allow-unreleased` POSIX field-test lane may instead accept a clean current
official branch head; that lane is not migration authority.

## Publication evidence and residual manual proof

- Passed: the full local gate on the exact reviewed release-preparation tree;
  merged commit `c8507312153620b9b30fe2c84980c62bccb3b25a` is tree-identical.
- Passed: deterministic exact-v0.1.0 POSIX migration fixtures, Windows
  setup/migration Pester coverage, and a fresh detached public-tag identity
  check.
- Passed: exact-head hosted Test, Nix, CodeQL, setup, parity, and container
  checks for pull request #61.
- Passed: redacted Gitleaks 8.30.1 scan across all 8 commits in
  `v0.2.0..v0.3.0` and the 4 downloaded logical proof artifacts (912 bytes);
  no leaks found.
- Passed: cache-free hosted release run
  [`29676087505`](https://github.com/luisgui1757/dotfiles/actions/runs/29676087505)
  across Ubuntu, Apple Silicon macOS, Windows, the Linux container, and all four
  stable logical proofs. Both POSIX lanes logged the exact immutable v0.3.0 tag
  identity.
- Passed: GitHub release `356273955` is immutable, latest, non-draft, and
  non-prerelease; its user-facing body matches the prepared notes.
- Open: real Apple Silicon owner-host, physical Linux, WSL2 split-host,
  redirected Windows, divergent stable packaged/Preview/Canary/portable
  Terminal, and desktop/visual rows in `tests/MANUAL.md`.

Initial cache-free run
[`29675684683`](https://github.com/luisgui1757/dotfiles/actions/runs/29675684683)
is failed diagnostic evidence, not release proof: macOS setup passed, but the
neocmake LSP probe missed its 45-second attach boundary. The identical
exact-head PR job had passed that probe, and one fresh full retry passed it in
39 seconds. No failed or cancelled logical artifact was reused.

The owner authorized publication with the existing real WSL, redirected-Windows,
divergent Windows Terminal, physical-Linux, Apple-Silicon owner-host, and visual
rows still open. They remain explicit residual evidence gaps in
`tests/MANUAL.md`; publication does not mark them complete.
